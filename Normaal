<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gekke Shit 🌈</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      overflow: hidden;
      background: linear-gradient(45deg, #ff00cc, #3333ff, #00ffcc, #ffcc00);
      background-size: 600% 600%;
      animation: bgmove 8s ease infinite;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      font-family: 'Comic Sans MS', cursive;
      color: white;
      text-shadow: 0 0 10px #fff;
    }

    @keyframes bgmove {
      0% {background-position: 0% 50%;}
      50% {background-position: 100% 50%;}
      100% {background-position: 0% 50%;}
    }

    h1 {
      font-size: 4em;
      animation: spin 4s linear infinite;
    }

    @keyframes spin {
      0% {transform: rotate(0deg) scale(1);}
      50% {transform: rotate(180deg) scale(1.3);}
      100% {transform: rotate(360deg) scale(1);}
    }

    .bouncer {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: white;
      position: absolute;
      animation: bounce 2s infinite ease-in-out;
    }

    @keyframes bounce {
      0%, 100% {transform: translateY(0);}
      50% {transform: translateY(-200px);}
    }

    /* Een paar willekeurige balletjes */
    .b1 { left: 20%; animation-delay: 0s; background: #ff0080; }
    .b2 { left: 40%; animation-delay: 0.3s; background: #00ffcc; }
    .b3 { left: 60%; animation-delay: 0.6s; background: #ffff00; }
    .b4 { left: 80%; animation-delay: 0.9s; background: #ff3300; }

    button {
      margin-top: 50px;
      padding: 15px 30px;
      font-size: 1.2em;
      border: none;
      border-radius: 8px;
      background: white;
      color: black;
      cursor: pointer;
      transition: 0.2s;
    }

    button:hover {
      background: black;
      color: white;
      transform: scale(1.2);
      box-shadow: 0 0 20px white;
    }
  </style>
</head>
<body>
  <h1>NIGGERS</h1>

  <div class="bouncer b1"></div>
  <div class="bouncer b2"></div>
  <div class="bouncer b3"></div>
  <div class="bouncer b4"></div>

  <button onclick="rainbow()">Klik voor gratis sexspeeltjes</button>

  <script>
    function rainbow() {
      document.body.style.animation = "bgmove 0.5s linear infinite";
      setTimeout(() => {
        document.body.style.animation = "bgmove 8s ease infinite";
      }, 3000);
      alert("virus wordt nu gedownload");
    }
  </script>
</body>
</html>
